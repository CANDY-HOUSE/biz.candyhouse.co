# 与 Amplify 构建环境保持一致：安装依赖时强校验各包的 engines 字段。
#
# Amplify preBuild 的 yarn 是强校验的：某个包的 engines.node 不满足当前 node 版本时，
# 会直接 "error Found incompatible module" 让 `yarn install --frozen-lockfile` 失败、
# 整个构建挂掉（这次就是 KVS 带的 jsdom@30 要 node 22+、Amplify 的 node 20 装不上）。
#
# 本地默认是宽松的（只警告不拦），所以这类问题本地发现不了，一路合到 master 才在
# Amplify 挂。开启后本地 `yarn install` 在加依赖时就会当场拦住同类引擎不兼容，
# 进不了 lockfile。
--check-engines true
