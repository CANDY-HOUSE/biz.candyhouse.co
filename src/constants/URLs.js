const url = 'https://jp.candyhouse.co';
const intro = `${url}/pages/sesamebiz-introduction-guide-new`;
const plans = `${url}/pages/sesame-biz-operation#biz_plans`;
const terms = `${url}/pages/sesamebiz_terms`;
const privacy = `${url}/pages/privacy_policy`;
const shop = `${url}/collections/frontpage`;

export const URLs = {
  url,
  intro,
  plans,
  terms,
  privacy,
  shop,
};
